---
title: "Between 1v1 kegg analysis"
date: "`r format(Sys.time(), '%d %B, %Y')`"
output:
  html_document:
    toc: yes
    toc_depth: 3
  pdf_document:
    toc: yes
    toc_depth: '3'
params:
    scrna: 'scrna object'
    cluster: "clusters to analyse"
    cluster_info: "cluster info"
    project: "name/tag of the project"
    savedir: "directory where rds files were saved"
    funcs: "analysis steps to be performed"
    report_data_folder: "folder with the output tables"
    report_tables_folder: "folder with the table elements"
    report_plots_folder: "folder with the plot elements"
    report_plots_folder_png: "folder with the png plot elements"
    report_plots_folder_pdf: "folder with the pdf plot elements"
    author: "author of document"
author: '`r params$author`'
---

## Cluster: `r colorize(params$cluster_info,"red")`

```{r echo=FALSE, include=FALSE}
library(dplyr)
library(kableExtra)
```


  


## A_MxCre vs B_MxCre  kegg up

```{r, out.width="100%", echo=FALSE}
fname=file.path(
  params$report_plots_folder_png,
  glue("kegg_up_genes_heatmap_vs_A_MxCre.vs.B_MxCre_cluster-{params$cluster}.png")
)
if(file.exists(fname)) knitr::include_graphics(fname)
```


```{r, out.width="100%", echo=FALSE}
### up genes top 10
flist = stringr::str_sort(grep(
  glue("top10-keggup_bar_vs_A_MxCre.vs.B_MxCre_cluster-{params$cluster}_p"),
  list.files(
    params$report_plots_folder_png,
    full.names=TRUE
  ),
  value=TRUE,
  fixed=TRUE
),numeric=TRUE)
knitr::include_graphics(flist)

```
  


## A_MxCre vs B_MxCre  kegg down

```{r, out.width="100%", echo=FALSE}
fname=file.path(
  params$report_plots_folder_png,
  glue("kegg_down_genes_heatmap_vs_A_MxCre.vs.B_MxCre_cluster-{params$cluster}.png")
)
if(file.exists(fname)) knitr::include_graphics(fname)
```


```{r, out.width="100%", echo=FALSE}
### down genes top 10
flist = stringr::str_sort(grep(
  glue("top10-keggdown_bar_vs_A_MxCre.vs.B_MxCre_cluster-{params$cluster}_p"),
  list.files(
    params$report_plots_folder_png,
    full.names=TRUE
  ),
  value=TRUE,
  fixed=TRUE
),numeric=TRUE)
knitr::include_graphics(flist)

```
  



